{% set filterOptionsHtml %}

  {{ govukCheckboxes({
    idPrefix: "filters[orgType]",
    name: "filters[orgType]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Organisation type",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: [{
      value: 'School',
      text: 'School'
    }, {
      value: 'MAT',
      text: 'MAT'
    }],
    values: data.filters.orgType
  }) }}

  


  {# {{ govukRadios({
  classes: "govuk-radios--inline govuk-radios--small",
  name: "Actor",
  fieldset: {
    legend: {
      text: "Participant",
      classes: "govuk-fieldset__legend--s"
    }
  },
  items: [
    {
      value: "Mentor",
      text: "Mentor"
    },
    {
      value: "ECT",
      text: "ECT"
    }
  ]
}) }} #}

{{ govukCheckboxes({
    idPrefix: "filters[trainingStatus]",
    name: "filters[trainingStatus]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Training status",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: [{
      value: 'Training',
      text: 'Training'
    },
    {
      value: 'Completed',
      text: 'Completed'
    },
    {
      value: 'Withdrawn',
      text: 'Withdrawn'
    },
    {
      value: 'Deferred',
      text: 'Deferred'
    }],
    values: data.filters.trainingStatus
  }) }}

  {{ govukDateInput({
  id: "filters[firstRegistered]",
  namePrefix: "filters[firstRegistered]",
  fieldset: {
    legend: {
      text: "First registerd",
      classes: "govuk-fieldset__legend--s"
    }
  },
  hint: {
    text: "For example, 27 3 2007"
  }
}) }}
  

{% endset %}

<div class="app-filter">
  <div class="app-filter__header">
    <div class="app-filter__header-title">
      <h2 class="govuk-heading-m">Filter</h2>
    </div>
    <div class="app-filter__header-action">

    </div>
  </div>
  <div class="app-filter__content">
    {% if selectedFilters.categories.length %}
      <div class="app-filter__selected">
        <div class="app-filter__selected-heading">
          <div class="app-filter__heading-title">
            <h2 class="govuk-heading-m govuk-!-margin-bottom-1">Selected filters</h2>
            <p class="govuk-body"><a class="govuk-link govuk-link--no-visited-state" href="/applications/clear-filters">Clear filters</a></p>
          </div>
        </div>
        {% for category in selectedFilters.categories %}
          <h3 class="govuk-heading-s govuk-!-margin-bottom-0">{{ category.heading.text }}</h3>
          <ul class="app-filter-tags">
            {% for item in category.items %}
              <li><a class="app-filter__tag" href="{{item.href}}"><span class="govuk-visually-hidden">Remove this filter</span> {{item.text}}</a></li>
            {% endfor %}
          </ul>
        {% endfor %}
      </div>
    {% endif %}
    <div class="app-filter__options">
      <form method="get" action="" novalidate>
        <button class="govuk-button" data-module="govuk-button">
          Apply filters
        </button>
        {{filterOptionsHtml | safe}}
      </form>
    </div>
  </div>
</div>
